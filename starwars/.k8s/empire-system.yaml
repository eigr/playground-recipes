---
apiVersion: spawn-eigr.io/v1
kind: ActorSystem
metadata:
  name: empire-system
  namespace: default
spec:
  externalInvocation:
    enabled: "true"
    externalConnectorRef: invocation-connection-ref-secret
  statestore:
    type: Native